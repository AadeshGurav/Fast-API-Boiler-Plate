version: '3.8'

services:
  app:
    build: .
    container_name: boiler_plate
    command: ["python3", "main.py"]
    volumes:
      - ./:/app
      - ./config.d:/app/config.d
    depends_on:
      - mongodb
      - redis
    ports:
      - "8000:8000"
    networks:
      - boiler-plate-network

  mongodb:
    image: bitnami/mongodb:latest
    container_name: boiler-plate-mongodb
    restart: always
    networks:
      - boiler-plate-network
    ports:
      - "18001:27017"
    volumes:
      - mongodb_data:/bitnami/mongodb
    environment:
      MONGODB_ROOT_PASSWORD: "boiler-plate"
      MONGODB_ENABLE_DIRECTORY_PER_DB: "yes"
      MONGODB_ENABLE_IPV6: "no"
      MONGODB_EXTRA_FLAGS: "--oplogSize=1024"
    deploy:
      resources:
        limits:
          memory: 2048M
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "mongosh --quiet -u root -p 'boiler-plate' --eval 'quit(db.runCommand({ ping: 1 }).ok ? 0 : 2)' || exit 1"
        ]
      interval: 30s
      timeout: 5s
      retries: 10
      start_period: 10s
    security_opt:
      - "no-new-privileges"
    sysctls:
      net.core.somaxconn: "65535"
    cap_drop:
      - ALL

  redis:
    image: redis:8.2.3-alpine
    container_name: boiler-plate-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - boiler-plate-network

volumes:
  mongodb_data:
  redis_data:

networks:
  boiler-plate-network:
    driver: bridge
